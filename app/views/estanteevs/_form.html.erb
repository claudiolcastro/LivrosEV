<%= form_with(model: estanteev, local: true) do |form| %>
  <% if estanteev.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estanteev.errors.count, "error") %> prohibited this estanteev from being saved:</h2>

      <ul>
      <% estanteev.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :estanteev_nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
